<% content_for(:html_title) { @genre.name } %>

<h3><%= @genre.name %> Games</h3>
<ul>
    <% @games.each do |game| %>
        <li><%= link_to game.name, game %></li>
    <% end %>
</ul>
<hr>
<%= will_paginate @games %>
